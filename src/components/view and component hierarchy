Login
-SigninForm
-SignupForm

Tweets
-SignoutButton
-TweetCreator
-TweetDisplay (for each tweet)
    -TweetEditor
    -TweetDeleteButton
    -TweetLikeButton
    -CommentCreator
    -CommentDisplay (for each comment)
        -CommentEditor
        -CommentDeleteButton

Profiles
-SignoutButton
-ProfileDisplay (for each profile)
    -ProfileEditor
    -ProfileDeleteButton
    -FollowUserButton